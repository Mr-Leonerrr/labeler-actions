name: publish

on: [push]

jobs:
  publish-action-docker-image:
    runs-on: ubuntu-latest
    environment: docker-image-builder

    steps:
      - uses: actions/checkout@master
        with:
          submodules: true

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Building pull-request-labeler docker image
        run: |
          docker build .  --tag ghcr.io/leonerrr/pull-request-labeler:1.0.1 --tag ghcr.io/leonerrr/pull-request-labeler:latest
          docker push ghcr.io/leonerrr/pull-request-labeler:1.0.1
          docker push ghcr.io/leonerrr/pull-request-labeler:latest
